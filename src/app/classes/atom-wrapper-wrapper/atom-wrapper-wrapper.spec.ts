import { AtomWrapperWrapper } from './atom-wrapper-wrapper';

describe('AtomWrapperWrapper', () => {
  it('should create an instance', () => {
    expect(new AtomWrapperWrapper()).toBeTruthy();
  });
});
